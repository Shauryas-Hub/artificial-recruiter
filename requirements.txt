# ============================================================================
# Redrob Ranker — Python Dependencies
# ============================================================================
# Python >= 3.10 required.
# Install: pip install -r requirements.txt
# ============================================================================

# Core data
numpy>=2.0.0
pandas>=2.0.0,<3.0.0
pyarrow>=12.0.0           # parquet I/O

# Embeddings (precompute only — not used in ranking step)
sentence-transformers>=2.2.2,<3.0.0
torch>=2.0.0               # sentence-transformers dependency

# Config parsing
pyyaml>=6.0

# Sandbox / demo UI
streamlit>=1.32.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Optional: for Jupyter notebooks
# jupyter>=1.0.0
# matplotlib>=3.7.0
# seaborn>=0.12.0
